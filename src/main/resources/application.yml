spring:
  application:
    name: mock-api
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

server:
  port: 8010
  servlet:
    context-path: /api

api:
  client:
    access-key: 123
    secret-key: 456
